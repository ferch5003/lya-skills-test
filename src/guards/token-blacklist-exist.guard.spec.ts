import { TokenBlacklistExistGuard } from './token-blacklist-exist.guard';

describe('TokenBlacklistExistGuard', () => {
  it('should be defined', () => {
    expect(new TokenBlacklistExistGuard()).toBeDefined();
  });
});
